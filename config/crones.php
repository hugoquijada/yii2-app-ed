<?php

use eDesarrollos\cron\Scheduler;

return function (Scheduler $schedule) {
  // $schedule->command('test')->everyMinute();
  // $schedule->command('test')->hourly();
  // $schedule->command('test')->daily(2, 0);
};
